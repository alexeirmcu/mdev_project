## Instructions for the AI Code Generator (System Prompt Extension)
*"Read this Software Architecture Document (SAD) carefully. Your task is to generate the full boilerplate skeleton for a .NET 8 Web API solution named `BasecampTripPlanner.sln`. Write all three `.csproj` files with the appropriate package dependencies (`MediatR`, `FluentValidation`, `Google.OrTools`, `Microsoft.EntityFrameworkCore.InMemory`, `Swashbuckle.AspNetCore`). Generate the complete directory structure following Clean Architecture principles. Write the complete `Program.cs` file using ASP.NET Core Minimal APIs mapping all 5 defined endpoints to MediatR Queries and Commands. Write all Domain Enums and Entities as specified. Implement MediatR Handlers, Commands, and Queries as position records. For the `GoogleOrToolsOptimizer` class in the Infrastructure layer, do not write actual constraint programming optimization logic; instead, inject dependencies and return a fully structured mock `TripPlanDto` populated with sample data based in Madrid, Spain, ensuring it groups activities into Morning, Afternoon, and Evening blocks. The final generated code must be perfectly clean, syntactically correct, and compile without errors under the .NET 8 SDK environment."*